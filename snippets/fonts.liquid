{% # theme-check-disable %}
{%- comment -%}
  PERF: Shopify theme fonts (Newsreader, Red Hat Display) are NOT used.
  Site uses self-hosted Outfit + Roboto via local-fonts.liquid.
  Preloading unused fonts wastes ~80-120KB of high-priority bandwidth.
  Removed preloads; font-face declarations remain in base.css for fallback.
{%- endcomment -%}
{% # theme-check-enable %}
