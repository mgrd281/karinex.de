<!-- Instant Google Indexing System -->
{%- if article -%}
  <!-- RSS Auto-Discovery -->
  <link rel="alternate" type="application/rss+xml" title="{{ shop.name }} - {{ blog.title }}" href="{{ shop.url }}{{ blog.url }}.atom">
  
  {%- comment -%} 
    Schema removed — BlogPosting is already output by article-layout.liquid.
    Duplicate NewsArticle + BlogPosting on the same page confuses Google.
  {%- endcomment -%}
{%- endif -%}

{%- comment -%} Ping scripts removed — use Google Search Console Indexing API instead {%- endcomment -%}
